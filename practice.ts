// type weatherDetailType = {
//   weather: string;
//   zipcode: string;
//   temp?: number;
// }

interface WeatherProps {
  weather: string;
  zipcode: string;
  temp?: number;
}

const weatherComponent = (props: WeatherProps) => {
}